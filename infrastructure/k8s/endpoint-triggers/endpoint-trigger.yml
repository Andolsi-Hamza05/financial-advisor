apiVersion: batch/v1
kind: CronJob
metadata:
  name: endpoint-trigger
spec:
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: curl
            image: curlimages/curl
            args:
            - curl
            - -X
            - POST
            - http://data-collection.default.svc.cluster.local:80/producer/scrape-and-send/
          restartPolicy: OnFailure


